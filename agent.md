# PDF 转 Markdown 与 AI 客服知识库项目

> 项目状态：Web MVP 0.2 已实现；`shennn123/pdf-knowledge-md` 私有仓库已创建，等待 SSH 公钥授权后推送
> 文档版本：0.11
> 最后更新：2026-07-29
> 文档用途：本文件是项目范围、决策、架构、风险和下一步工作的当前事实源。出现新结论时直接更新相关章节，避免并存互相冲突的旧方案。

## 1. 项目背景

公司数字化部门计划建设 AI 客服聊天 Agent。现有知识源以 PDF 为主，包括操作手册、规范文件等。项目希望将 PDF 离线转换为结构化 Markdown，并用于后续检索增强生成（RAG），减少在线问答中的无效上下文、改善检索和引用质量。

当前工作区已经完成可运行的转换管线和内网网站 MVP，并通过自动化测试及真实浏览器操作验证。尚未取得公司真实业务 PDF，因此 Docling、PaddleOCR 和复杂中文文档质量仍未完成业务基准验证。

### 1.1 当前实现状态

已实现：

- Python 包和 `pdf-to-md` CLI。
- FastAPI + Jinja2 + 原生 JavaScript 内网网站 MVP。
- 单文件 PDF 上传、任务列表、任务详情、状态轮询、质量结果和解析器状态展示。
- Markdown 安全纯文本预览、`.md` 下载和完整审计 ZIP 下载。
- SQLite 任务数据库、进程内后台线程池和存活/就绪健康检查。
- 上传扩展名、PDF 文件头、文件大小和业务文档 ID 校验；服务端使用随机任务目录保存上传文件。
- 显式稳定业务文档 ID、相对来源 URI 和原始文件名审计字段，消除了跨服务器路径变化导致主键变化的问题。
- `inspect`、单文件 `convert`、目录 `batch` 和 `engines` 命令。
- 原生文本、混合型和扫描型 PDF 的文本密度分类。
- `auto`、pypdf、Docling、PaddleOCR PP-StructureV3 可插拔引擎接口。
- pypdf 文本型 PDF 基线转换，可在当前环境直接运行。
- 编号标题恢复、重复页眉/页脚清理、边缘页码清理和源页码注释。
- SHA-256 去重、不可变版本目录、原子 `current.json` 指针和强制重跑。
- `source.pdf`、`document.md`、`manifest.json` 审计产物。
- 页面映射、文本密度、乱码比例和扫描件风险质量门禁。
- `review_required` 候选默认不发布，避免低质量结果覆盖当前版本。
- 7 个自动化测试，覆盖正常发布、重复跳过、扫描件拦截、稳定业务文档 ID、批量任务输出目录排除、Web 上传、状态、预览和下载。
- 使用 ReportLab 生成两页 PDF，结合 Poppler 渲染逐页检查，并验证转换结果。
- 使用系统 Edge 完成真实浏览器上传、任务完成、质量评分和 Markdown 预览验证。

当前未完成或未验证：

- 当前环境未安装 Docling 和 PaddleOCR，其适配代码已完成但尚未进行模型级运行验证。
- PaddleOCR 图片资源尚未持久化到 `assets/`。
- 网站当前只支持单文件上传，尚未实现批量上传、统一登录、ACL、人工批准、版本回滚和知识库推送。
- 当前任务在 Web 进程内线程池执行；生产所需独立 worker/任务队列、超时和资源隔离尚未实现。
- 尚未实现知识分块和 Embedding/BM25 索引。
- 尚未使用公司真实中文手册、复杂表格和扫描件进行准确率评测。

### 1.2 可迁移交付要求

已确认该工具必须迁移到公司内网，并且不能把客户端安装包作为员工使用方式。最终用户通过公司浏览器访问内网网站，不需要安装 Python、解析器或桌面程序。

服务器端仍需要由运维或研发部署一次。最终推荐交付方式：

1. 内网 Web 服务作为唯一用户入口。
2. Docker Compose 或公司现有容器平台作为服务器部署方式。
3. 无网络环境使用离线容器镜像、模型权重、配置、许可证清单和校验文件迁入内网。
4. 源码 wheel 仅可作为容器内部构建产物，不作为员工安装或使用方式。

当前代码使用 `pathlib`、环境变量和相对默认路径，没有写死开发机执行路径。内网网站 MVP 已实现；源码目录可以压缩传到公司电脑，通过 Python 虚拟环境完成开发验证，并在后续推送到公司私有 GitHub 仓库。

可迁移交付尚未完成：

- Web MVP 已实现，但生产级认证、ACL、队列、数据库、备份和反向代理尚未实现。
- 尚未锁定完整服务端依赖版本和提供哈希校验。
- 尚无 Dockerfile、容器健康检查和非 root 运行配置。
- 尚无离线容器镜像与 Docling/PaddleOCR 模型缓存包。
- 尚未在 Linux、另一台 Windows、无网络环境和 CPU-only 环境做迁移测试。
- PaddlePaddle 的 CPU/GPU 平台差异尚未拆分部署配置。
- CLI 和网站已支持显式业务 `document_id`；迁移时应沿用该业务 ID。CLI 省略该参数时仍保留路径派生的兼容行为，不应作为跨机器长期主键。
- `manifest.json` 已增加可迁移的来源 URI 和原始文件名；本机源路径仅保留为审计字段。
- 尚无统一 TOML/YAML 配置文件，部分阈值仍通过命令行参数设置。
- 尚未生成 SBOM、第三方许可证清单和模型权重清单。
- 尚无安装/升级/回滚脚本和迁移验收脚本。

### 1.3 内网网站方案

网站方案可行，当前 MVP 已实现其中的单文件核心闭环。目标用户流程：

1. 使用公司浏览器访问内网地址。
2. 拖拽或选择 PDF；当前 MVP 为单文件，批量上传待实现。
3. 填写业务文档 ID和分类；版本、生效日期和权限标签待实现。
4. 网站异步执行分析、转换和质量检查。
5. 用户查看任务进度、质量评分、警告及 Markdown 预览。
6. 质量通过后自动发布；审核人员批准 `review_required` 候选的界面待实现。
7. 下载单份 Markdown、完整 ZIP，或推送到 AI 客服知识库。

当前技术栈：

- 后端：FastAPI，直接复用现有 Python `ConversionPipeline`。
- 前端：Jinja2 + 原生 JavaScript，无独立 Node/React 构建链。
- 元数据库：MVP 使用 SQLite；多人并发和生产环境计划切换 PostgreSQL。
- 文件存储：MVP 使用本地 `storage/`；生产可切换公司 NAS 或兼容 S3 的内网对象存储。
- 任务执行：MVP 使用同进程线程池；生产计划使用 Redis 队列加独立转换 worker，避免 OCR 阻塞 Web 请求。
- 入口代理：公司现有 Nginx/网关，统一提供 HTTPS、上传大小限制和访问日志。
- 身份认证：优先接入公司 LDAP/AD、OIDC 或现有统一身份认证；未接入认证前不得开放给全公司。

网站页面状态：

- 已实现：单 PDF 上传、任务列表、状态、文档详情、Markdown 预览、质量报告、Markdown/ZIP 下载和解析器状态。
- 待实现：登录/用户识别、批量任务、版本历史、重新转换、人工发布、回滚、管理员配置和完整审计日志。

内网安全要求：

- 限制文件类型、单文件大小、批量数量和解压行为。
- 上传文件使用随机服务端文件名并与原始文件名分离。
- 转换任务运行在受限工作目录或独立 worker 中。
- 增加恶意文件扫描、超时、内存/CPU 限额和失败清理。
- 下载和预览必须执行身份认证与文档权限校验。
- Markdown 预览必须防止原始 HTML/脚本注入。
- 记录上传、转换、审核、下载、发布和回滚审计日志。
- 明确保留期限和源 PDF 删除策略。

### 1.4 公司 GitHub 交付与部署流程

已确认预期交付流程：

1. 将项目源码压缩或通过批准的介质传到公司电脑。
2. 使用公司 GitHub 账号，将源码推送到公司组织管理的私有仓库。
3. GitHub 仅用于源码、配置模板、测试和部署流程管理。
4. 从私有仓库部署到公司内网服务器，员工访问部署后的内网网站。

GitHub Pages 只适合静态网站，不能运行本项目的 Python、PDF/OCR、任务队列和持久化后端，因此不得将 GitHub Pages 作为正式部署目标。

已选择 GitHub 自动部署作为最终方式，不采用日常手工部署。当前决定是先完成和验证功能，Docker、GitHub Actions 和 self-hosted runner 配置延后实施。最终由公司内网服务器或同网络的 GitHub Actions self-hosted runner，在合并到受保护的 `main` 分支后构建、测试并更新服务。

当前新增的短期目标是先将现有源码发布到用户个人 GitHub 的私有仓库 `shennn123/pdf-knowledge-md` 进行预览。该操作只发布源码，不代表 FastAPI 网站已经获得公网运行环境；GitHub Pages 仍不能运行本项目。私有远端仓库和本地 `main` 首次提交已经创建，业务 PDF、虚拟环境、`storage/` 和临时文件已确认排除。由于当前网络只能访问 GitHub API 和官方 SSH 443，不能访问普通 Git HTTPS 地址，已在用户目录生成项目专用 SSH 密钥；GitHub 登录仍需增加 `admin:public_key` 权限后才能登记公钥并完成推送。

计划中的自动化流水线：

```text
功能分支 / Pull Request
        |
        v
代码检查 + 自动化测试（不接触生产数据）
        |
        v
审核并合并到受保护 main
        |
        v
production 环境审批（由公司策略决定是否启用）
        |
        v
内网 self-hosted runner
        |
        v
构建容器 -> 更新服务 -> 健康检查
        |
        +-> 成功：保留新版本
        |
        +-> 失败：恢复上一容器版本并报告失败
```

部署流水线必须：

- 使用公司组织的私有仓库。
- 使用专用 runner 标签，例如 `self-hosted`、`linux`、`x64`、`pdf-md-prod`。
- 生产 runner 不执行来自 fork 或不受信任分支的任意代码。
- `main` 开启分支保护，要求测试通过和代码审核。
- deploy job 只在 `main` 或手工回滚工作流触发。
- 配置 concurrency，避免两个部署同时修改生产服务。
- 生产 `.env`、证书和数据库凭据保留在内网服务器或公司密钥系统，不写入仓库。
- 部署后调用 `/health/live` 和 `/health/ready`，失败则停止发布并回滚。
- 保存部署版本、Git commit SHA、执行人、时间和健康检查结果。
- runner 使用专用低权限系统账号，不能与员工日常办公账号共用。

启用自动部署前必须确认：

- 内网 runner 能否通过 HTTPS 443 主动访问公司 GitHub/GitHub.com。
- 公司账号是否有私有仓库及 runner 的管理权限，或是否由 IT 提供组织级 runner。
- 内网目标服务器操作系统、CPU/GPU、Docker/容器平台能力。
- 内网域名、端口、TLS 证书、NAS/数据库和统一认证方式。

仓库必须保持私有，并遵守公司 GitHub 组织策略。不得提交：

- 业务 PDF、转换后的 Markdown 和 `knowledge/`。
- `.env`、密码、Token、证书和数据库备份。
- Docling/PaddleOCR 模型权重，除非公司明确允许并采用专用制品库或 Git LFS。
- 上传临时文件、日志和审计数据。

仓库应提交：

- `src/`、网站代码、模板和静态资源。
- 自动化测试。
- Dockerfile、Compose 配置和 `.env.example`。
- 数据库迁移脚本。
- GitHub Actions 工作流。
- 部署、升级、回滚、安全和许可证文档。

当前本地目录尚未初始化为 Git 仓库；计划在网站和部署文件完成后进行仓库初始化，或由公司电脑创建私有仓库后再导入。现有 `.gitignore` 已禁止提交业务 PDF、输入目录内容、知识库输出、模型、数据、密钥和环境变量文件。

## 2. 执行结论

项目整体可行性：**高**。

推荐做法不是“所有 PDF 统一调用一个转换器”，而是建设一条可审计的文档处理管线：

1. 识别 PDF 类型：原生文本、扫描件、混合型、复杂版式。
2. 普通文档使用本地结构化解析。
3. 扫描件、复杂表格、多栏或公式文档转入 OCR/版面解析后备引擎。
4. 对 Markdown 做规则化清洗、质量评分和抽样人工复核。
5. 通过内容哈希和版本号增量发布。
6. 按标题层级进行语义分块并重建受影响的检索索引。
7. 在线客服只读取检索命中的少量片段，不读取整份 Markdown。

建议首选组合：

- 默认解析器：**Docling**
- 中文扫描件和复杂版面后备：**PaddleOCR PP-StructureV3**
- 高复杂度文档对照实验：**MinerU**
- 暂不将 PyMuPDF4LLM 作为公司闭源产品的默认依赖，除非法务确认 AGPL 合规或采购商业许可

## 3. 需要澄清的关键认知

“PDF 改成 Markdown 就一定显著减少 token”并不完全准确。

大模型实际消耗的输入 token 主要取决于在线请求中提交了多少文本，而不是源文件扩展名。如果当前系统每次把整份 PDF 解析结果或大量页面放入上下文，那么离线转换、去重、分块和精准检索会显著降低 token；如果现有系统已经对 PDF 做了等价的高质量解析和 RAG，单纯把结果保存成 `.md` 未必带来明显节省。

本项目的主要价值应定义为：

- 避免在每次提问时重复解析 PDF。
- 去除页眉、页脚、页码、目录重复项和版面噪声。
- 保留标题、列表、表格、图片说明等语义结构。
- 让知识库按章节和主题精确分块。
- 支持来源页码、版本、有效期和权限追溯。
- 只向模型提供与问题相关的片段。

因此，成功指标不能只有“成功生成 Markdown”，还必须衡量在线 token、检索命中和回答正确率。

## 4. 推荐总体架构

```text
PDF 来源目录/对象存储
        |
        v
文件发现 -> 哈希去重 -> 类型与难度分类
        |                    |
        |                    +-> 扫描/复杂版式 -> OCR/版面后备引擎
        v
默认结构化解析器
        |
        v
统一中间结构（标题、段落、表格、图片、页码、坐标）
        |
        v
Markdown 规范化 -> 自动质检 -> 低置信度人工复核
        |
        v
版本化发布（PDF + MD + assets + manifest）
        |
        v
语义分块 -> Embedding/BM25 -> 向量与关键词索引
        |
        v
检索 -> 重排 -> 权限过滤 -> AI 客服回答与来源引用
```

### 4.1 建议目录产物

每份源文档不应只有一个 `.md` 文件，建议保留：

```text
knowledge/
  <document_id>/
    current.json
    versions/
      <version_id>/
        source.pdf
        document.md
        manifest.json
        assets/
```

实现采用不可变 `versions/` 和原子更新的 `current.json`。`current.json` 是当前已发布版本的权威指针；候选版本质量不合格时仍可保留供审核，但不会更新该指针。这比直接替换整个 `current/` 目录更容易保证发布一致性和审计完整性。

`manifest.json` 至少包含：

- `document_id`
- 原文件名和来源路径
- 源文件 SHA-256
- 文档版本、发布日期、业务生效日期
- 转换器名称、版本和参数
- 页数、语言、PDF 类型
- 转换时间和耗时
- 质量评分、告警和人工复核状态
- Markdown 内容哈希
- 权限标签、部门、知识类别

## 5. 解析方案比较

| 方案 | 适用场景 | 优点 | 主要限制 | 企业许可注意 | 建议定位 |
|---|---|---|---|---|---|
| Docling | 原生文本、表格、常见扫描件和多格式文档 | 结构化能力完整；可 OCR；可表格解析；本地部署；代码为 MIT | 首次加载模型较重；复杂中文扫描件仍需实测 | 代码 MIT；所用模型仍需分别核对许可 | 默认引擎 |
| PaddleOCR PP-StructureV3 | 中文扫描件、表格、公式、多栏和版面恢复 | 中文 OCR 生态成熟；支持表格、公式、阅读顺序和 Markdown 输出 | 部署依赖较多；模型推理资源和速度需压测 | Apache-2.0 | OCR/复杂版式后备 |
| MinerU | 复杂 PDF、表格、公式、图片丰富文档 | 面向 LLM/RAG 输出 Markdown/JSON；复杂文档能力强 | 资源占用、版本变化和部署复杂度需实测 | 当前为基于 Apache-2.0 的自定义许可，含商业门槛和在线服务标识等附加条件，需法务审查 | POC 对照或高难文档引擎 |
| PyMuPDF4LLM | 原生文本型 PDF、追求轻量和速度 | 接口简单；支持 Markdown/JSON、页面分块、表格和 OCR 路由 | 复杂版式上限需实测 | AGPL-3.0/商业双许可；闭源商业使用应先完成法务评估 | 技术基线，不作为默认依赖 |
| 商业文档解析 API | 希望快速上线且允许文件出域 | 运维少；通常提供 SLA 和成熟 OCR | 持续调用成本、厂商锁定、数据合规和网络依赖 | 以采购合同和数据处理协议为准 | 合规允许时的备选 |

官方资料：

- [Docling 官方站点](https://www.docling.ai/)；[Docling MIT License](https://github.com/docling-project/docling/blob/main/LICENSE)
- [PaddleOCR PP-StructureV3 文档](https://www.paddleocr.ai/v3.6.0/en/version3.x/pipeline_usage/PP-StructureV3.html)；[PaddleOCR Apache-2.0 License](https://github.com/PaddlePaddle/PaddleOCR/blob/main/LICENSE)
- [MinerU 官方仓库](https://github.com/opendatalab/mineru)；[MinerU 当前许可](https://github.com/opendatalab/MinerU/blob/master/LICENSE.md)
- [PyMuPDF4LLM 官方文档](https://pymupdf.readthedocs.io/en/latest/pymupdf4llm/index.html)；[PyMuPDF4LLM 官方仓库与许可说明](https://github.com/pymupdf/pymupdf4llm)

## 6. 三种落地方案

### 方案 A：轻量单引擎 MVP

使用 Docling 完成所有文档的本地转换，加规则清洗和人工抽检。

适合：

- 首批 PDF 大多是可复制文本。
- 表格和扫描件比例低。
- 希望两周左右快速得到可测原型。

优点是组件少、许可证宽松、维护简单。缺点是遇到低质量扫描件或复杂中文表格时容易积累人工修复任务。

### 方案 B：本地混合解析管线（推荐）

Docling 作为默认引擎；系统通过文本密度、图片覆盖率、乱码率、表格数量和质量分数判断是否转交 PaddleOCR PP-StructureV3。对少量特别复杂的文档使用 MinerU 做对照，确认收益后再决定是否纳入生产。

适合：

- 操作手册和规范的质量差异较大。
- 数据不能离开公司网络。
- 希望质量、成本和可维护性平衡。

这是当前推荐方案。

### 方案 C：商业 API 优先

文档上传到合规通过的商业解析服务，内部只维护调度、质检、版本发布和索引。

适合：

- 上线时间比长期成本更重要。
- 已完成数据出域、保密和供应商风险评估。
- 缺少 GPU/OCR 运维能力。

它能缩短开发时间，但不会消除质检、版本控制、权限和 RAG 分块工作。

## 7. Markdown 输出规范

建议输出满足以下规则：

- 一份源 PDF 对应一个主 Markdown，图片单独存放在 `assets/`。
- 标题只按语义层级使用 `#` 到 `######`，不能按字体大小机械生成。
- 删除重复页眉、页脚和孤立页码，但保留源页码映射。
- 列表保持有序/无序结构。
- 简单表格输出为 GitHub Flavored Markdown；复杂合并单元格表格可使用 HTML。
- 表格不能跨知识分块丢失表头。
- 图片保留相对路径、图题、源页码；对客服有业务意义的流程图需补充人工校验的文字说明。
- 公式使用 LaTeX；代码使用带语言标记的 fenced code block。
- 不允许利用生成式模型静默“补全”原文。任何 LLM 清洗结果都应能与源页对照并留下审计记录。
- 文件统一 UTF-8，换行和空白做规范化。

## 8. 知识库分块与在线检索

推荐先按标题路径切分，再按长度细分，而不是按固定页数切分。

初始实验参数可采用：

- 每块约 500-1000 token。
- 相邻块重叠 10%-15%，但标题路径和短步骤列表优先保持完整。
- 表格尽量整体保留；过大时按行组拆分，并在每块重复表头。
- 每块附带 `document_id`、版本、标题路径、源页码、有效期、权限标签和内容哈希。
- 检索采用关键词/BM25 与向量混合召回，再使用重排模型筛选。
- 最终送给大模型的片段数量由验证集调优，不能固定追求更大的 `top_k`。
- 回答必须返回文档名、版本、章节和页码，方便客服或用户核验。

文档更新时只删除该 `document_id` 旧版本对应的索引项，再写入新版本；发布过程使用临时版本和原子切换，避免用户读到半更新状态。

## 9. 增量更新策略

1. 定时扫描目录、监听对象存储事件，或由文档平台主动推送。
2. 计算源文件 SHA-256；哈希未变化则跳过。
3. 转换到临时版本目录。
4. 完成页面完整性、乱码、标题、表格和资源链接检查。
5. 质量不达标则保持旧版本在线，并进入人工复核队列。
6. 质量达标后写入新索引，再原子切换 `current`。
7. 保留旧版本用于回滚和审计。
8. 对“业务生效日期”与“文件上传时间”分别建模，避免新上传的旧制度覆盖现行制度。

## 10. 可行性分项评估

| 维度 | 评级 | 说明 |
|---|---|---|
| 技术实现 | 高 | 已有成熟开源解析、OCR、Markdown 和 RAG 组件 |
| 普通文本 PDF 准确度 | 高 | 通常可直接恢复段落、标题和列表 |
| 扫描件准确度 | 中高 | 受分辨率、倾斜、印章、底纹和字体影响 |
| 复杂表格/流程图 | 中 | 是主要难点，需要后备引擎和人工抽检 |
| 中文支持 | 高 | PaddleOCR 等方案成熟，但公司真实模板仍需测试 |
| Token 降本 | 中高 | 取决于当前基线；主要收益来自离线处理、去噪、分块与精准检索 |
| 企业部署 | 高 | 可全部本地化；需完成模型和依赖许可证清单 |
| 运维复杂度 | 中 | 需要监控失败任务、版本一致性、索引同步和质量漂移 |

## 11. 主要风险与控制措施

| 风险 | 影响 | 控制措施 |
|---|---|---|
| OCR 错字改变操作步骤或参数 | 客服给出错误指引 | 关键字段规则校验；低置信度拦截；关键文档人工复核 |
| 表格行列错位 | 条件、价格或权限解释错误 | 表格专项测试；保留源页截图；复杂表使用 HTML/结构化 JSON |
| 页眉页脚污染 | 检索重复、token 增加 | 跨页重复检测和规则清洗 |
| 标题层级错误 | 分块语义断裂 | 结合目录、字体、编号规则和版面模型恢复 |
| 新旧制度冲突 | 回答引用失效规则 | 文档版本、业务生效时间、废止状态和优先级元数据 |
| 转换“成功”但内容缺页 | 知识缺失且难察觉 | 源页数与产物页映射 100% 校验；空页和异常短页告警 |
| LLM 后处理产生幻觉 | 内容被悄然篡改 | MVP 禁用生成式补全；后续只允许可审计、可回放的受限修复 |
| 权限信息泄露 | 合规事故 | 索引前附 ACL；检索时按用户身份过滤，不能只在生成阶段过滤 |
| 开源许可不兼容 | 法务和商业风险 | 建立 SBOM；上线前审查代码、模型、权重及传递依赖许可 |

## 12. POC 验收指标

以下是建议的首轮目标值，不是未经测试的承诺；应在样例集上调整：

- 页级完整率：100%，所有源页都有处理记录或明确的空白页标记。
- 原生文本字符准确率：不低于 99%。
- 扫描件关键字段准确率：不低于 98%，并单独统计数字、单位、型号和步骤序号。
- 标题层级正确率：不低于 95%。
- 关键表格单元格准确率：不低于 95%。
- 图片和表格引用链接有效率：100%。
- 黄金问题集检索 Recall@5：不低于 90%。
- 有答案问题的引用正确率：不低于 95%。
- 与现有方案相比，单次问答平均输入 token 至少下降 30%，同时回答正确率不能下降。
- 增量更新失败时，线上旧版本保持可用。

### 12.1 Token 降本测量

对同一组真实问题做 A/B：

- A：当前 PDF 知识库流程。
- B：规范化 Markdown + 结构化分块 + 混合检索。

至少记录：

- 每问输入/输出 token。
- 召回块数、召回文本 token、重复 token。
- 检索 Recall@k 和 MRR/nDCG。
- 答案正确率、引用正确率、拒答正确率。
- 文档离线转换时间和成本。
- 索引时间、存储量和单次查询延迟。

只有在回答质量不下降的前提下，token 减少才算有效收益。

## 13. 推荐实施计划

当前进度：

- 阶段 0 的评测框架雏形已具备，但仍缺公司真实样本和客服黄金问题集。
- 阶段 1 已完成解析器适配接口和 pypdf 基线验证；Docling、PaddleOCR、MinerU 的同样本盲测尚未开始。
- 阶段 2 已完成转换、基础质检、版本发布和失败隔离的第一版；任务队列、分块、索引和审核能力尚未完成。
- 内网网站已完成单文件核心闭环 MVP；生产认证、独立队列、批量上传和部署流水线尚未完成。

### 阶段 0：样本与基线（3-5 个工作日）

- 收集 30-50 份有代表性的 PDF。
- 覆盖原生文本、扫描、混合、表格、多栏、图片流程、印章和低清晰度。
- 从中选 100-200 个真实客服问题，标注标准答案、来源章节和页码。
- 测量当前系统 token、正确率、检索和时延基线。

### 阶段 1：解析器 POC（5-8 个工作日）

- 对 Docling、PaddleOCR PP-StructureV3 和 MinerU 做同样本盲测。
- 自动计算文字、页完整性和链接指标。
- 人工评分标题、表格、阅读顺序和流程图。
- 确定默认引擎、后备路由阈值和许可结论。

### 阶段 2：MVP 管线（1-2 周）

- 建立批量上传、哈希去重、任务队列、转换、质检和版本发布。
- 实现统一 Markdown 规范、manifest、日志和失败重试。
- 接入分块、混合检索和来源引用。
- 提供最小人工复核界面或审核清单。

### 阶段 3：试运行与上线（1-2 周）

- 在一个业务域灰度运行。
- 对失败文档、错误回答和 token 成本持续复盘。
- 完成 ACL、监控、告警、回滚、备份和 SBOM/许可证审查。

在有 1-2 名工程师、范围受控且基础设施可用的前提下，通常可在约 3-5 周形成可灰度的 MVP；复杂审批、GPU 采购或大量特殊模板会延长周期。

## 14. 建议的技术实现边界

MVP 应包含：

- CLI 或内部 API 批量转换。
- 文档类型检测和解析器路由。
- Markdown、assets 和 manifest 产出。
- 自动质量检查和失败报告。
- 内容哈希、版本、原子发布和回滚。
- 可配置的语义分块与知识库增量更新。
- 结构化日志、指标和许可证清单。

MVP 暂不包含：

- 通用可视化 PDF 编辑器。
- 对所有复杂流程图的自动语义理解。
- 无审计的 LLM 自动改写。
- 一开始就支持所有 Office、网页和图片格式。
- 在没有真实评测集时宣称固定准确率或 token 节省比例。

## 15. 当前建议与下一步

当前建议：

1. 采用方案 B“本地混合解析管线”。
2. 当前 pypdf 基线只用于让文本型 PDF 流程立即可运行，不改变 Docling 作为生产默认解析器候选的建议。
3. 在隔离环境安装并验证 Docling，再接入和验证 PaddleOCR PP-StructureV3 扫描件后备路线。
4. MinerU 只进入 POC，对高难样本比较质量，同时进行许可审查。
5. 将“Markdown 生成质量”和“客服 RAG 效果”作为两个独立验收层。

下一步所需输入：

- 20-50 份脱敏后的代表性 PDF。
- 当前知识库/RAG 的技术栈和调用方式。
- PDF 总量、平均页数、每日更新量。
- 扫描件、表格和流程图的大致占比。
- 是否要求完全离线、可用 CPU/GPU 环境。
- 权限隔离、保密等级和留存期限要求。

下一阶段工程工作：

1. 用真实样本运行现有 `inspect/convert/batch`，建立失败样本清单。
2. 安装 Docling，完成与 pypdf 的同样本 Markdown、页面完整性和耗时比较。
3. 选择扫描件样本安装 PaddleOCR，验证中文 OCR、表格和阅读顺序。
4. 增加 `assets/` 图片落盘、复杂表格结构校验和人工审核状态。
5. 实现按标题路径的知识分块及 JSONL 输出，再对接现有知识库索引。
6. 建立客服黄金问题集，对 token、Recall@5、答案与引用正确率做 A/B。
7. 使用公司真实样本补齐网站批量任务、人工审核、认证和 ACL。
8. 功能稳定后实现 Docker Compose、GitHub Actions self-hosted runner 自动部署和离线镜像迁移，并在独立内网 Windows/Linux 环境执行验收。

当前可从仓库根目录运行：

```powershell
$env:PYTHONPATH = "src"
python -m uvicorn pdf_to_md.web.app:app --host 127.0.0.1 --port 8000

$env:PYTHONPATH = "src"
python -m pdf_to_md engines
python -m pdf_to_md inspect .\input\manual.pdf
python -m pdf_to_md convert .\input\manual.pdf --output .\knowledge
python -m pdf_to_md batch .\input --recursive --output .\knowledge
```

## 16. 决策记录

| 日期 | 状态 | 决策/结论 |
|---|---|---|
| 2026-07-29 | 已确认 | 创建 `agent.md` 作为项目当前事实源，并通过 `AGENTS.md` 要求后续任务开头读取、在决策变化时同步更新 |
| 2026-07-29 | 预评估 | 项目技术可行性高，但 token 收益必须通过真实 RAG A/B 验证 |
| 2026-07-29 | 建议，待样本验证 | 优先采用 Docling + PaddleOCR PP-StructureV3 的本地混合方案 |
| 2026-07-29 | 待确认 | 具体解析器、质量阈值、资源规格、部署形式和排期需在样本 POC 后确定 |
| 2026-07-29 | 已实现 | 完成 MVP 0.1：CLI、PDF 分类、pypdf 基线、Docling/PaddleOCR 适配器、质量门禁、SHA-256 去重、不可变版本和原子当前指针 |
| 2026-07-29 | 已确认 | `current.json` 作为已发布版本权威指针；`review_required` 候选默认不得覆盖当前版本 |
| 2026-07-29 | 已验证 | 4 个自动化测试通过；两页 PDF 经 Poppler 逐页视觉核对，转换结果页面映射完整并正确清理重复页眉和页码 |
| 2026-07-29 | 待验证 | Docling、PaddleOCR、复杂中文表格、扫描件和真实客服 RAG 指标仍需业务样本验证 |
| 2026-07-29 | 已确认 | 工具必须迁移到公司内网，最终用户不得通过客户端安装包使用；采用浏览器访问的内网网站作为交付形态 |
| 2026-07-29 | 建议，待实现 | 网站后端采用 FastAPI 复用现有转换管线，首版前端采用 Jinja2 + HTMX，服务器通过容器和离线镜像迁入内网 |
| 2026-07-29 | 已确认 | 项目将先迁移到公司电脑，再使用公司 GitHub 账号推送到公司私有仓库，并从仓库部署到内网服务器 |
| 2026-07-29 | 已确认 | GitHub 只管理源码和部署流程，不存储业务 PDF、知识库产物、密钥、审计数据或未经批准的模型权重 |
| 2026-07-29 | 已确认 | 正式部署采用 GitHub Actions + 内网 self-hosted runner 自动部署；Pull Request 只运行测试，合并到受保护 `main` 后才允许更新内网服务 |
| 2026-07-29 | 已确认 | 当前先完成产品功能和本地验证，Docker 与 GitHub Actions 部署实现延后；最终代码仍托管在公司私有 GitHub 仓库 |
| 2026-07-29 | 已实现 | 完成 Web MVP 0.2：单文件上传、稳定业务文档 ID、SQLite 任务记录、后台转换、质量结果、Markdown 预览与下载、审计 ZIP 和健康检查 |
| 2026-07-29 | 已验证 | 7 个自动化测试通过，并使用 Edge 完成上传、转换、质量结果和预览的真实浏览器验证 |
| 2026-07-29 | 已确认 | 在正式公司内网部署前，先把当前源码发布到用户个人 GitHub 私有仓库进行预览；这一步只托管代码，不使用 GitHub Pages 运行 FastAPI |
| 2026-07-29 | 当前阻塞 | GitHub CLI 已安装，但 DNS 解析后的 `github.com:443` TCP 连接失败，设备登录接口超时；等待恢复合规网络或正确配置命令行代理后继续登录和推送 |
| 2026-07-29 | 已解决 | GitHub CLI 已通过浏览器设备流程登录账号 `shennn123`；沙箱外验证 GitHub API 访问正常 |
| 2026-07-29 | 已确认 | 个人预览仓库采用私有仓库 `shennn123/pdf-knowledge-md`；本地仓库使用 `main` 分支，业务数据与开发运行产物不进入版本控制 |
| 2026-07-29 | 已完成 | 已创建私有远端仓库 `https://github.com/shennn123/pdf-knowledge-md` 和本地首次提交；普通 Git HTTPS 端点仍不可达，因此尚未推送 |
| 2026-07-29 | 当前阻塞 | GitHub SSH 443 可达，项目专用密钥已在本机生成，但当前 OAuth 登录缺少 `admin:public_key` 权限；等待用户完成一次 `gh auth refresh` 权限确认 |
