# PDF 转 Markdown 项目

> 项目状态：Web MVP 0.6 混合 OCR 已实现并通过真实模型与网页流程验证，待公司脱敏样本验收
> 文档版本：1.8
> 最后更新：2026-07-29
> 文档用途：项目范围、已确认决策、实现状态、风险和下一步工作的当前事实源。

## 1. 当前目标

为公司 AI 客服知识库提供一个可迁移的 PDF 转 Markdown 工具。最终用户只需：

1. 选择“完整转换”或“重点摘要”。
2. 在浏览器上传一份 PDF。
3. 等待系统自动转换。
4. 查看与原 PDF 的文本相似度和改动说明。
5. 预览或下载 Markdown。

用户明确要求保持功能简单。网站不再展示或要求：

- 业务文档 ID、分类、版本、生效日期。
- 解析器选择。
- 质量评分、人工审核和发布操作。
- 任务列表、版本历史和 ZIP 审计包。
- RAG、Embedding、BM25 或知识库推送。

## 2. 已实现

### 2.1 用户功能

- 单文件 PDF 拖拽或选择上传。
- 拖拽通过浏览器 `DataTransfer` 真正写入文件输入框；非 PDF 和多文件拖入会被拒绝。
- 自动选择后台转换引擎。
- 转换中状态自动刷新。
- Markdown 安全纯文本预览。
- 以原 PDF 文件名下载 `.md`。
- 文件扩展名、PDF 文件头和上传大小校验。
- 结果页提供“返回上一级”；顶部品牌入口始终可返回首页。
- 转换前选择模式：
  - `full`：尽量保留全部可提取正文。
  - `summary`：抽取标题、步骤、关键要求、数字、日期、型号和编号。
- 结果页显示文本保留相似度、计算方法和主要改动。

### 2.2 自动完整性检查

转换后自动比较 PDF 可提取原文和 Markdown：

- 页数与源页标记是否完整。
- 每页单独检查正文、关键事实和关键要求，防止总体覆盖率掩盖单页遗漏。
- 整体正文四字符片段覆盖率是否达到 85%，单页是否达到 80%。
- 数字、日期、百分比、型号和编号覆盖率是否达到 98%。
- 包含“必须、禁止、不得、注意、要求、条件、步骤”等信号词的关键句
  是否全部保留。

检查结果分为：

- `complete`：自动规则未发现明显遗漏。
- `incomplete`：正文、关键事实或关键句可能遗漏。
- `limited`：一页或多页没有可提取原文或源页映射，无法逐页验证，需要 OCR 或更完整的解析器。

该检查不调用生成式模型，不总结、改写或补写原文。它是遗漏检测，不是语义
正确性的绝对证明。

### 2.3 重点摘要与对比报告

- 重点摘要采用抽取式规则，只使用转换结果中已存在的原文行。
- 每页保留标题、前导内容、列表/步骤、关键要求、表格行和包含数字/型号的句子。
- 摘要按 PDF 源页码重新组织，不新增或改写业务事实。
- 结果记录原文行数、抽取行数、省略行数和选择原因。
- “相似度”定义为 PDF 可提取原文四字符片段在结果中的覆盖率，是文本保留
  指标，不是语义正确率。
- “主要改动”列出结构化、页码注释、主动省略、重点选择和关键事实保留情况。

### 2.4 内部实现

- FastAPI 后端。
- Jinja2 + 原生 JavaScript 单页式用户流程。
- pypdf 文本型 PDF 基线转换。
- 可选 Docling 和 PaddleOCR 引擎适配器。
- SQLite 保存转换任务。
- 后台线程执行转换。
- SHA-256、不可变版本目录和 `current.json` 原子指针仍作为内部可靠性机制，
  但不展示给普通用户。
- `/health/live` 和 `/health/ready` 健康检查。
- SQLite 任务表增加 `mode` 字段，并自动迁移旧数据库，旧任务默认为 `full`。
- 自动分析逐页文字密度，仅将文字不足的页面交给 PaddleOCR PP-StructureV3。
- OCR 页面保留 `source-page` 页码，记录提取方式、识别字符数和平均置信度。
- OCR 页面以 OCR 提取全文作为转换/摘要保留率基线，并明确不将其描述为图片识别准确率。
- 首页在 OCR 运行时缺失时显示管理员提示；健康检查返回 OCR 可用状态。
- 提供联网预热、离线轮子与模型缓存打包、内网离线安装脚本。
- 提供 `docs/验证逻辑与准确度说明.md`，说明完整性验证、文本保留相似度、
  OCR 置信度及真实 OCR 准确率评测边界。
- 14 项自动化测试全部通过。
- 已使用系统 Edge 完成真实浏览器上传、转换、自动检查、预览和下载入口验证。
- 已使用 Edge `DataTransfer` 模拟真实拖拽，确认非 PDF 拒绝、PDF 文件接收、
  转换提交、检查明细和返回上一级全部正常。

## 3. 当前技术边界

- pypdf 负责有文字层页面，PaddleOCR 负责文字不足页面；部署环境仍需安装 PaddleOCR 运行时并预热模型，或使用项目提供的离线包脚本迁移轮子和模型缓存。
- 当前开发机已安装 PaddlePaddle 3.3.1 和 PaddleOCR 3.7.0，并完成 Windows CPU 真实模型推理；为规避当前 oneDNN 转换兼容问题，OCR 管线显式关闭 MKLDNN。
- Docling、PaddleOCR 尚未使用公司脱敏真实样本完成模型级准确率验证。
- 自动完整性检查能发现明显文字遗漏，不能判断复杂表格行列、流程图含义或
  OCR 中“看起来合理但实际错误”的文字。
- 重点摘要是抽取式而非生成式，能避免幻觉，但不能像大模型一样合并同义内容
  或理解隐含业务重要性。
- 网站尚未接入公司统一认证，只能用于本地或受控测试环境。
- 当前后台使用同进程线程，不是生产级任务队列。

## 4. 数据与迁移

项目不能以员工客户端安装包作为最终交付方式。最终用户通过公司内网浏览器
访问，服务器由研发或运维部署。

当前代码使用相对路径和环境变量，不写死开发机目录。源码可复制到另一台
Windows/Linux 电脑，重新创建 Python 虚拟环境后运行。

不得提交到 GitHub：

- 业务 PDF、转换后的 Markdown。
- `storage/`、`knowledge/`、上传临时文件和数据库。
- `.env`、Token、密码、证书和私钥。
- `.venv/`、模型权重和缓存。

## 5. GitHub 状态

- 本地 Git 仓库已初始化，分支为 `main`。
- 已创建私有仓库：`https://github.com/shennn123/pdf-knowledge-md`。
- `main` 已推送并跟踪 `origin/main`。
- GitHub API 与官方 SSH 443 可达，普通 Git HTTPS 443 不可达。
- 已在 `C:\Users\13179\.ssh\id_ed25519_pdf_md` 生成项目专用 SSH 密钥。
- 已将公钥作为仅限 `shennn123/pdf-knowledge-md` 的可写 Deploy Key 添加，
  不需要扩大 GitHub 账号的全局公钥管理权限。
- `origin` 使用 `ssh://git@ssh.github.com:443/shennn123/pdf-knowledge-md.git`，
  本地仓库通过项目专用私钥推送。

GitHub 当前只负责源码托管。GitHub Pages 不能运行 FastAPI，正式公司内网
部署配置继续延后。

## 6. 验收标准

- 首页提供完整转换和重点摘要两种模式，之后只有 PDF 上传主操作。
- 用户无需填写任何元数据或选择引擎。
- 转换完成后直接显示相似度、主要改动、Markdown 预览和下载按钮。
- 页面不出现人工审核、质量评分、版本发布或 ZIP 下载。
- 非 PDF 和伪造 PDF 被拒绝。
- 自动检查可发现测试样本中缺失的关键数字、关键要求和单独缺失页面。
- 14 项自动化测试通过。
- 混合 PDF 中只有文字不足页面调用 OCR，成功页保留原页码并进入完整性检查。
- OCR 失败页明确列出，不能被标为完整成功。
- 真实浏览器拖拽 PDF 后文件输入框包含该文件并可完成转换。
- 结果页能返回上一级，所有功能页均有回到首页的入口。
- 业务数据不进入 Git 提交。

## 7. 下一步

1. 使用公司脱敏真实 PDF 验证普通文本、表格和扫描件，并记录错误样本。
2. 根据真实样本结果决定是否增加表格结构识别；当前版本优先保证文字和关键事实，不承诺复杂表格行列完全还原。
3. 功能验收后再讨论公司内网部署，不提前加入认证、队列等复杂功能。

## 8. 决策记录

| 日期 | 状态 | 决策/结论 |
|---|---|---|
| 2026-07-29 | 已确认 | `agent.md` 是项目当前事实源，范围或状态变化时必须同步更新 |
| 2026-07-29 | 已实现 | MVP 0.1 完成 CLI、pypdf 基线、可选引擎适配、质量门禁和版本化输出 |
| 2026-07-29 | 已实现 | Web MVP 0.2 完成上传、任务、质量报告、预览和下载 |
| 2026-07-29 | 已确认 | 最终交付为公司内网网站，源码托管在私有 GitHub，不使用客户端安装包 |
| 2026-07-29 | 已创建 | 个人预览私有仓库 `shennn123/pdf-knowledge-md` 已创建但尚未推送 |
| 2026-07-29 | 已确认 | 用户将范围收敛为“上传 PDF → 转 Markdown → 自动检查 → 预览/下载” |
| 2026-07-29 | 已取消 | 网站不再提供人工审核、质量评分、业务元数据、解析器选择、任务列表和 ZIP 审计包 |
| 2026-07-29 | 已实现 | Web MVP 0.3 完成简化界面及正文、关键事实、关键句自动完整性检查 |
| 2026-07-29 | 已验证 | 8 项自动化测试通过；Edge 真实浏览器验证只显示上传、自动检查、Markdown 预览和下载，已移除复杂功能入口 |
| 2026-07-29 | 已实现 | Web MVP 0.4 将完整性检查升级为逐页检查，并增加问题页和无法验证页定位 |
| 2026-07-29 | 已验证 | 10 项自动化测试通过；Edge 真实 `DataTransfer` 拖拽、非 PDF 拒绝、PDF 转换、检查明细和返回上一级均通过 |
| 2026-07-29 | 已完成 | 使用仓库级可写 Deploy Key 和 GitHub 官方 SSH 443 将 `main` 推送到私有仓库 `shennn123/pdf-knowledge-md` |
| 2026-07-29 | 已确认 | 转换前由用户选择“完整转换”或“重点摘要”；结果必须显示与原 PDF 的文本相似度和主要改动 |
| 2026-07-29 | 已实现 | Web MVP 0.5 增加抽取式重点摘要、模式数据库迁移、文本保留相似度和改动报告 |
| 2026-07-29 | 已验证 | 13 项自动化测试通过；Edge 验证完整模式与摘要模式，摘要能省略普通背景行并保留关键型号、时限和警告码 |
| 2026-07-29 | 已推送 | 双模式 Web MVP 0.5 已提交并推送到私有仓库 `shennn123/pdf-knowledge-md` 的 `main` 分支 |
| 2026-07-29 | 已确认 | 扫描页 OCR 必须补齐，不能仅提示用户“无法验证” |
| 2026-07-29 | 已实现 | Web MVP 0.6 增加逐页混合 OCR、OCR 页码与置信度元数据、OCR 保留率基线及内网离线迁移脚本 |
| 2026-07-29 | 已验证 | 14 项自动化测试通过；模拟 PP-StructureV3 验证仅对缺少文字层页面调用 OCR，且摘要后仍保留 OCR 页映射 |
| 2026-07-29 | 已完成 | 新增 `docs/验证逻辑与准确度说明.md`，统一解释验证阈值、四字符片段相似度、OCR 置信度及其局限 |
| 2026-07-29 | 已验证 | PaddlePaddle 3.3.1 与 PaddleOCR 3.7.0 在 Windows CPU 环境通过官方运行检查和真实模型推理；当前配置关闭 MKLDNN 以规避 oneDNN 属性转换错误 |
| 2026-07-29 | 已验证 | 两页混合 PDF 的真实网页流程通过：第 1 页读取文字层，第 2 页自动 OCR，OCR 平均置信度 97.04%，文本保留相似度 99%，关键事实与关键要求全部保留，下载文件哈希一致 |
